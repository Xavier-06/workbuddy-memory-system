# 新手上手指南

> 本指南通过真实场景教你什么时候写记忆、怎么写、以及怎么确认记忆系统在工作。

---

## 一、常见场景示范

### 场景 1：用户告诉你一个偏好

**情境**：用户说"以后别叫我'大哥'，叫我 Xavier"。

**你会话末尾这样写**：

```markdown
# 2026-04-23 工作日志

- 用户纠正称呼：不要叫"大哥"，叫 Xavier（来自今日对话）
```

**extractMemories 会处理**，自动更新 user_profile.md：

```markdown
---
name: 用户画像
description: Xavier 的背景、偏好
type: user
---

## 沟通偏好

- 称呼：叫 Xavier，不叫"大哥"、不叫其他昵称
```

### 场景 2：你犯了个错，用户纠正你

**情境**：你发了一个文件给用户，用户说"不是这个，给我原文件，不要带日期的副本"。

**立即写入 corrections.md**（不是等会话结束）：

```markdown
## [2026-04-23] 交付文件用了副本而非原文件

**问题：** 用户要原文件，我发了带日期的副本

**纠正：** 原文件发原文件路径，不要用 `cp` 副本。必须确认文件是原始状态。

**规则：** 交付文件给用户前，确认是原始文件路径，不是副本。
```

### 场景 3：项目做了一个关键决策

**情境**：你帮用户做了一个技术选型，决定用 PostgreSQL 而不是 MySQL。

**写入项目记忆**（如果没有就创建）：

```markdown
---
name: XX项目技术选型
description: 数据库选型决策
type: project
---

## 技术选型

- **数据库：PostgreSQL** — 选型原因：JSON 支持更好，支持全文检索，未来扩展到向量检索更容易
- 放弃 MySQL — 原因：JSON 支持弱，生态不如 PG
```

### 场景 4：了解到一个外部系统

**情境**：用户提到他们用 Notion 管理文档，告诉你页面 ID。

**写入 reference**：

```markdown
---
name: Notion 文档管理
description: 用户使用 Notion 管理项目文档
type: reference
---

## 访问方式

- 地址：notion.so
- 页面 ID：{{用户给的ID}}
- 用途：项目文档管理
```

### 场景 5：发现一个可复用的经验

**情境**：你发现"用 cron 跑定时任务比 WorkBuddy 自动化更可靠"。

**立即写入 self-improving/memory.md**（HOT 规则）：

```markdown
## HOT: 定时任务优先用 cron

**场景：** 需要定时执行的任务（每日蒸馏、备份等）

**规则：** 优先用系统 cron，比 WorkBuddy 自动化任务更稳定，不依赖会话

**Why：** WorkBuddy 自动化依赖 AI 会话是否活跃，cron 是系统级服务，挂了会收到系统通知
```

---

## 二、新手最容易踩的 5 个坑

### 坑 1：把所有对话都写进记忆

**错误做法**：每天日志写成流水账，连"今天问了天气"都记。

**正确做法**：只记录有**持久价值**的信息。用户偏好、项目决策、规则——这些值得记。闲聊不值得。

**自检问题**：这条记忆，1 个月后还有用吗？没有就别写。

---

### 坑 2：同一件事写多个地方

**错误做法**：用户偏好同时写在 `user_profile.md` 和每日日志里。

**正确做法**：每个事实只存一处。写了 user_profile 就删每日日志里的那条。

**自检问题**：这个信息在别的地方已经有记录了吗？

---

### 坑 3：写了记忆但不更新 MEMORY.md 索引

**错误做法**：新建了一个主题文件，但忘记在 MEMORY.md 加指针。

**正确做法**：新建主题文件后，立刻在 MEMORY.md 索引加一行。

**自检问题**：MEMORY.md 里有没有指向不存在文件的悬空指针？

---

### 坑 4：自我纠正滞后

**错误做法**：被用户纠正了，说"好的我记住了"，然后没有写进 corrections.md。

**正确做法**：被纠正的当下立即写进 corrections.md，不要依赖记忆。

**自检问题**：上次被用户纠正是什么？现在那条纠正存在吗？

---

### 坑 5：向量搜索替代了文件记忆

**错误做法**：只依赖向量搜索，忽略文件记忆系统。

**正确做法**：向量搜索是加速层，文件记忆是真相源。新知识先写文件，向量搜索负责找出来。

**自检问题**：上次手动读文件记忆是什么时候？如果很久没有，说明可能过度依赖向量了。

---

## 三、如何确认记忆系统在工作

### 验证方法 1：会话接续测试

1. 在一个会话里告诉 AI 一个偏好或决定
2. 结束会话
3. 开始新会话
4. 问 AI："你还记得我上次说的 XX 吗？"

**预期**：AI 应该能准确回忆起来。

**如果 AI 说"不知道"**：检查：
- 记忆写对位置了吗？
- MEMORY.md 索引有这条吗？
- AI 启动时有没有执行 SessionStart 仪式？

---

### 验证方法 2：每日日志检查

```bash
# 查看今天的日志
cat ~/.workbuddy/memory/$(date +%Y-%m-%d).md

# 检查是否有新内容
ls -la ~/.workbuddy/memory/*.md | tail -10
```

**预期**：每天应该有日志，内容和当天工作相关。

---

### 验证方法 3：extractMemories 验证

1. 手动触发一次 extractMemories（或等自动化任务跑）
2. 检查主题文件是否有更新
3. 检查 MEMORY.md 索引是否一致

```bash
# 检查索引行数是否合理
wc -l ~/.workbuddy/memory/MEMORY.md

# 检查是否有悬空指针（引用了不存在的文件）
grep -E '^\- \[' ~/.workbuddy/memory/MEMORY.md | while read line; do
  file=$(echo "$line" | grep -oP ']\([^)]+\)' | tr -d ']()')
  if [ ! -f "$HOME/.workbuddy/memory/$file" ]; then
    echo "DANGLING: $file"
  fi
done
```

---

### 验证方法 4：Self-Improving 验证

1. 故意在一个任务上犯错
2. 看 AI 是否主动写入 corrections.md
3. 下次遇到类似场景，AI 是否引用了 corrections.md 的规则

---

## 四、第一周行动计划

| 天 | 做什么 |
|----|--------|
| Day 1 | 初始化记忆系统（init-memory-system.sh），填身份模板 |
| Day 2 | 配置所有自动化任务 |
| Day 3 | 手动写 3 条记忆（用户偏好、项目决策、Self-Improving） |
| Day 4 | 运行 extractMemories，检查主题文件是否更新 |
| Day 5 | 故意给 AI 一个纠正，看它是否写入 corrections.md |
| Day 6 | 验证会话接续：说一个偏好，换个会话问 AI 记不记得 |
| Day 7 | 检查向量记忆状态（如果可用） |

---

## 五、常见问题

**Q：不知道该写哪个类型（user/project/feedback/reference）怎么办？**

A：按这个顺序判断：
1. 是用户告诉你的偏好？→ user
2. 是用户纠正你的？→ feedback
3. 是项目相关的？→ project
4. 是外部系统的位置？→ reference
5. 都不是但值得记？→ 可能是 project 或 reference

**Q：写了记忆但 AI 还是不知道？**

A：排查顺序：
1. MEMORY.md 索引有这条吗？
2. 文件路径对吗？
3. AI 启动时读了吗？（检查 AI 日志）
4. >1 天的记忆，AI 有没有重新验证？

**Q：向量搜索找不到文件？**

A：运行 `vecmem.py sync` 做增量同步，或 `vecmem.py index --full` 做全量重建。
